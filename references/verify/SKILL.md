---
name: duanxianxia-verify
version: "1.0.0"
description: |
  竞价计划复盘验证：读当日 9:27 竞价计划（~/.hermes/cron/output/e1652881ece0/ 当日计划 md）的关注标的表，
  用腾讯批量行情/日K 逐票对照实际走势，判定计划偏差（反包失败/承接失败、一字加速验证、数据缺失票补算），
  输出 [{票, 计划判断, 实际走势, 偏差类型, 教训}] 写 verify_YYYY-MM-DD.json（并入复盘 JSON「竞价计划验证」字段）；
  连续 2 日同类偏差 → 把修正规则建议写入 duanxianxia-auction 的形态判定表（skill 自迭代）。
  使用时机（即使未明说也应触发）：盘后验证竞价计划、计划偏差归因、"计划 vs 实际"复盘、用户问「竞价计划对不对」时。
origin: custom (duanxianxia 子技能, 2026-09-16 拆分)
---

# duanxianxia-verify 竞价计划复盘验证（12.6）

- **目的**：对**当日早上 9:27 竞价计划**（`~/.hermes/cron/output/e1652881ece0/<YYYY-MM-DD>_*.md`，取当日最新一份）逐票验证判断对错，把"计划 vs 实际"的偏差写成教训，持续校准竞价判定规则。
- **验证方法**：读当日计划的关注标的表（每票有 竞价高开%/形态/操作），用腾讯批量行情/日K 取实际走势对照：
  - **反包失败/承接失败**：计划判"承接极强，可跟进"但开盘后无力承接回落、或判"弱转强候选低吸"但实际走跌（实例：百大集团 9/14 计划表外判断"主线延续"，实际 9/14 跌停开仅拉 4% 后 9/15 直接跌停；瑞尔特 9/15 计划首选弱转强低吸，实际当日 -10%）→ 记「计划偏差」；
  - **一字加速票验证**：计划判"不参与/只排不追"的票，实际开板/封死情况；
  - **数据缺失票**：计划中"竞价数据缺失"的票，补算当日实际开盘/竞价表现，评估"若当时有数据是否应参战"。
- **输出**：
  - `~/.cache/duanxianxia/verify_YYYY-MM-DD.json`：`[{票, 计划判断, 实际走势, 偏差类型, 教训}]`（供 19:10 复盘汇总并入复盘 JSON 的「竞价计划验证」字段 + 推送摘要一行：计划票名 | 计划判断 | 实际走势 | 偏差（遗漏参战标的/误判形态））；
  - 连续 2 日同类偏差 → 把修正规则**建议**写入报告末尾，并说明建议更新到 `duanxianxia-auction` 的形态判定表（skill 自迭代，用户可确认后改）。
